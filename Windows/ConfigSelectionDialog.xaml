<Window x:Class="CMDevicesManager.Windows.ConfigSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CMDevicesManager.Windows"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Width="620" Height="520"
        Background="Transparent"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="{DynamicResource AppFontFamily}"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- ✅ 深蓝紫渐变配色方案 - 与 ColorPickerWindow 一致 -->
        <LinearGradientBrush x:Key="WindowBg" StartPoint="0,0" EndPoint="1,1" Opacity="0.98">
            <GradientStop Color="#1A1F3A" Offset="0.0"/>
            <GradientStop Color="#2D1B3D" Offset="1.0"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="CardBg" StartPoint="0,0" EndPoint="1,1" Opacity="0.6">
            <GradientStop Color="#1A1F3A" Offset="0.0"/>
            <GradientStop Color="#2A2045" Offset="1.0"/>
        </LinearGradientBrush>
        
        <SolidColorBrush x:Key="CardBorder" Color="#5B7FFF" Opacity="0.5"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubtitleBrush" Color="#B8C5FF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#5B7FFF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#7B8FFF"/>
        <SolidColorBrush x:Key="CyanAccentBrush" Color="#00D9FF"/>
        <SolidColorBrush x:Key="HoverOverlay" Color="#20FFFFFF"/>
        <SolidColorBrush x:Key="ListSelectedBg" Color="#22FFFFFF"/>
        <SolidColorBrush x:Key="SearchIconBrush" Color="#B8C5FF"/>

        <!-- ✅ 紫色光晕阴影 -->
        <DropShadowEffect x:Key="WindowShadow" 
                          Color="#6B4FFF"
                          Direction="270" 
                          ShadowDepth="8"
                          Opacity="0.5" 
                          BlurRadius="20"/>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- ✅ 主按钮样式 - 蓝紫渐变 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#5B7FFF" Offset="0"/>
                        <GradientStop Color="#7B8FFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#5B7FFF" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#7B8FFF" Offset="0"/>
                                            <GradientStop Color="#8B9FFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#4B6FEF" Offset="0"/>
                                            <GradientStop Color="#5B7FFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ 次要按钮样式 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource SubtitleBrush}"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <!-- ✅ 搜索框样式 -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TitleBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource CyanAccentBrush}"/>
        </Style>

        <!-- ✅ ListViewItem 样式 -->
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Root"
                                CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource HoverOverlay}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource ListSelectedBg}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ 滚动条样式 -->
        <SolidColorBrush x:Key="ScrollThumbNormal" Color="#5B7FFF" Opacity="0.4"/>
        <SolidColorBrush x:Key="ScrollThumbHover"  Color="#7B8FFF" Opacity="0.6"/>
        <SolidColorBrush x:Key="ScrollThumbDrag"   Color="#00D9FF" Opacity="0.8"/>

        <Style x:Key="RoundedThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect Color="#5B7FFF" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollThumbHover}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollThumbDrag}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="{StaticResource ScrollThumbNormal}"/>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Foreground" Value="{StaticResource ScrollThumbNormal}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root" Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True"
                                   Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Width="0" Height="0"
                                                  Command="{x:Static ScrollBar.LineUpCommand}"
                                                  Opacity="0" IsHitTestVisible="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Width="0" Height="0"
                                                  Command="{x:Static ScrollBar.LineDownCommand}"
                                                  Opacity="0" IsHitTestVisible="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb"
                                           Style="{StaticResource RoundedThumbStyle}"
                                           Margin="0"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="Root"  Property="Width" Value="10"/>
                                <Setter TargetName="Thumb" Property="MinHeight" Value="24"/>
                                <Setter TargetName="Thumb" Property="Width" Value="8"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="1,2,1,2"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="Root"  Property="Height" Value="10"/>
                                <Setter TargetName="Thumb" Property="MinWidth" Value="24"/>
                                <Setter TargetName="Thumb" Property="Height" Value="8"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="2,1,2,1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Row="0" Grid.Column="0"
                                                    Margin="{TemplateBinding Padding}"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Row="0" Grid.Column="1"
                                       Orientation="Vertical"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Width="10"
                                       Margin="0"
                                       Background="Transparent"
                                       SmallChange="16" LargeChange="64"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="1" Grid.Column="0"
                                       Orientation="Horizontal"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Height="10"
                                       Margin="0"
                                       Background="Transparent"
                                       SmallChange="16" LargeChange="64"/>

                            <Border Grid.Row="1" Grid.Column="1"
                                    Background="Transparent"
                                    x:Name="Corner"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ✅ 外层容器 - 深蓝紫渐变 + 紫色光晕 -->
    <Border CornerRadius="16"
            Background="{StaticResource WindowBg}"
            BorderBrush="{StaticResource CardBorder}"
            BorderThickness="1.5"
            SnapsToDevicePixels="True"
            Effect="{StaticResource WindowShadow}">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ✅ 标题栏 -->
            <Grid Grid.Row="0" Height="40" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="选择配置"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource TitleBrush}"
                           VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00D9FF" 
                                          ShadowDepth="0" 
                                          BlurRadius="10" 
                                          Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <Button Grid.Column="1"
                        Width="32" Height="28"
                        Content="✕"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnClose_Click"
                        ToolTip="关闭"
                        Margin="0"/>
            </Grid>

            <!-- ✅ 搜索框 -->
            <Border Grid.Row="1"
                    Margin="0,12,0,10"
                    CornerRadius="10"
                    Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1.5"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Path Data="M12,11 L16,15 M9,3 A6,6 0 1 1 8.999,3.01"
                          Stroke="{StaticResource SearchIconBrush}"
                          StrokeThickness="2"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          Width="28" Height="18"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0"/>
                    <TextBox x:Name="SearchBox"
                             Grid.Column="1"
                             Style="{StaticResource SearchBoxStyle}"
                             TextChanged="SearchBox_TextChanged"
                             ToolTip="搜索"/>
                </Grid>
            </Border>

            <!-- ✅ 配置列表 -->
            <ListView x:Name="ListViewConfigs"
          Grid.Row="2"
          Background="Transparent"
          BorderThickness="0"
          Foreground="{StaticResource TitleBrush}"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          MouseDoubleClick="ListViewConfigs_MouseDoubleClick">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:ConfigSelectionDialog+ConfigListItem}">
                        <Border Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}"
                    BorderThickness="1.5"
                    CornerRadius="12"
                    Padding="10"
                    SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- ✅ 预览图 -->
                                <Grid Grid.Column="0"
                          Width="100"
                          Height="100"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center">
                                    <Border CornerRadius="10"
                                Background="#1A1F3A"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1.5"
                                SnapsToDevicePixels="True"
                                ClipToBounds="True">
                                        <Image Source="{Binding PreviewImage}"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   SnapsToDevicePixels="True"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>

                                <!-- ✅ 配置信息 -->
                                <StackPanel Grid.Column="1"
                                Margin="14,2,4,2"
                                VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TitleBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Path}"
                                   Margin="0,6,0,0"
                                   FontSize="11"
                                   Foreground="{StaticResource SubtitleBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ShowPathInList, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>

                                <!-- ✅ 删除按钮 -->
                                <Button Grid.Column="2"
                            Width="36"
                            Height="36"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            ToolTip="删除此配置"
                            Tag="{Binding}"
                            Click="DeleteConfig_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.8">
                                                        <GradientStop Color="#D84F4F" Offset="0.0"/>
                                                        <GradientStop Color="#B21818" Offset="1.0"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="bd"
                                                    CornerRadius="8"
                                                    Background="{TemplateBinding Background}">
                                                            <Border.Effect>
                                                                <DropShadowEffect Color="#D84F4F" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                                            </Border.Effect>
                                                            <TextBlock Text="&#xE74D;"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="16"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="bd" Property="Background">
                                                                    <Setter.Value>
                                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                            <GradientStop Color="#E86565" Offset="0.0"/>
                                                                            <GradientStop Color="#C72828" Offset="1.0"/>
                                                                        </LinearGradientBrush>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="bd" Property="Background">
                                                                    <Setter.Value>
                                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                            <GradientStop Color="#B23F3F" Offset="0.0"/>
                                                                            <GradientStop Color="#A01010" Offset="1.0"/>
                                                                        </LinearGradientBrush>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- ✅ 底部按钮 -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,14,0,0">
                <Button x:Name="BtnLoad"
                        Width="120"
                        Content="加载"
                        Click="BtnLoad_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
                <Button x:Name="BtnCancel"
                        Width="100"
                        Margin="10,0,0,0"
                        Content="取消"
                        Click="BtnCancel_Click"
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>