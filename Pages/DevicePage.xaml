<Page x:Class="CMDevicesManager.Pages.DevicePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      FontFamily="{DynamicResource AppFontFamily}"
      Title="Devices" Loaded="Page_Loaded" Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Devices}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Margin="24" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- Whole tile is still a Button for hover visuals; click on specific option buttons only -->
                    <Button Cursor="Hand"
                            Padding="0"
                            Margin="12"
                            Width="280"
                            Height="420"
                            Background="Transparent"
                            BorderBrush="{x:Null}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <ControlTemplate.Resources>
                                    <Style x:Key="DeviceOptionButtonStyle" TargetType="Button">
                                        <Setter Property="Background" Value="#CC222222"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="16,8"/>
                                        <Setter Property="Margin" Value="0,8,0,0"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FF3A3A3A"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="#FF555555"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ControlTemplate.Resources>
                                
                                <Grid x:Name="Root" ClipToBounds="True">
                                    <!-- Card base -->
                                    <Border x:Name="Card"
                                            CornerRadius="16"
                                            Background="#121212">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="4" Opacity="0.35" />
                                        </Border.Effect>
                                    </Border>

                                    <!-- Device image -->
                                    <Image Source="{Binding ImagePath}"
                                           Stretch="UniformToFill"
                                           SnapsToDevicePixels="True"
                                           ClipToBounds="True"/>

                                    <!-- Top gradient -->
                                    <Border VerticalAlignment="Top"
                                            Height="80"
                                            CornerRadius="16,16,0,0"
                                            Background="#80000000"/>

                                    <!-- Title -->
                                    <StackPanel Orientation="Vertical" Margin="16" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="White"
                                                   FontSize="18"
                                                   FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="#CCFFFFFF"
                                                   FontSize="12"
                                                   Opacity="0.75"/>
                                    </StackPanel>

                                    <!-- Settings glyph -->
                                    <Border x:Name="HoverGlyphContainer"
                                            Width="32" Height="32"
                                            CornerRadius="16"
                                            Background="#33000000"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="12"
                                            Opacity="0">
                                        <TextBlock Text="&#xE713;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="14"/>
                                    </Border>

                                    <!-- Dark overlay -->
                                    <Border x:Name="HoverOverlay"
                                            Background="#99000000"
                                            CornerRadius="16"
                                            Opacity="0"/>

                                    <!-- Center action list -->
                                    <StackPanel x:Name="HoverOptions"
                                                Width="180"
                                                Opacity="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                        <Button Style="{StaticResource DeviceOptionButtonStyle}"
                                                Content="屏幕自定义"
                                                Tag="{Binding}"
                                                Click="ScreenCustomize_Click"/>
                                        <Button Style="{StaticResource DeviceOptionButtonStyle}"
                                                Content="播放模式"
                                                Tag="{Binding}"
                                                Click="PlaybackMode_Click"/>
                                        <Button Style="{StaticResource DeviceOptionButtonStyle}"
                                                Content="固件信息"
                                                Tag="{Binding}"
                                                Click="FirmwareInfo_Click"/>
                                    </StackPanel>

                                </Grid>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.55"/>
                                        <Setter TargetName="HoverGlyphContainer" Property="Opacity" Value="1"/>
                                        <Setter TargetName="HoverOptions" Property="Opacity" Value="1"/>
                                        <Setter TargetName="Card" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="6" Opacity="0.5"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
