<Page x:Class="CMDevicesManager.Pages.DeviceSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CMDevicesManager.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="Transparent"
      Title="DeviceSettings">

    <Page.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="CardBg" Color="#232834"/>
        <SolidColorBrush x:Key="CardBgAlt" Color="#1B202B"/>
        <SolidColorBrush x:Key="CardBorder" Color="#2E3747"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubtitleBrush" Color="#B8C0CC"/>
        <SolidColorBrush x:Key="ValueBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#38D6BD"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F45B89"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>

        <!-- Shadow effect for cards -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="Black" 
                          Direction="270" 
                          ShadowDepth="8" 
                          Opacity="0.4" 
                          BlurRadius="20"/>

        <!-- Card style -->
        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <!-- Section header style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TitleBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Label style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource SubtitleBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Value style -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Toggle button style -->
        <Style x:Key="ToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#444444"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <!-- Track -->
                            <Border x:Name="Track"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="14"
                                    Width="50"
                                    Height="28"/>
                            <!-- Thumb -->
                            <Border x:Name="Thumb"
                                    Background="White"
                                    CornerRadius="12"
                                    Width="24"
                                    Height="24"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0">
                                <Border.RenderTransform>
                                    <TranslateTransform X="0"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Thumb" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="22"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Thumb" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2AC4AA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1FA088"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly">
        <StackPanel Margin="24">
            <!-- Page Title -->
            <TextBlock Text="Device Settings"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{StaticResource TitleBrush}"
                       Margin="0,0,0,32"/>

            <!-- Firmware Information Card -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="Firmware Information" Style="{StaticResource SectionHeaderStyle}"/>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Current Version -->
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Current Version:"
                                   Style="{StaticResource LabelStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   x:Name="FirmwareVersionText"
                                   Text="v1.2.3"
                                   Style="{StaticResource ValueStyle}"/>

                        <!-- Status -->
                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Status:"
                                   Style="{StaticResource LabelStyle}"
                                   Margin="0,12,0,0"/>
                        <StackPanel Grid.Row="1" Grid.Column="1"
                                    Orientation="Horizontal"
                                    Margin="0,12,0,0">
                            <Ellipse x:Name="StatusIndicator"
                                     Width="8" Height="8"
                                     Fill="{StaticResource SuccessBrush}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,8,0"/>
                            <TextBlock x:Name="StatusText"
                                       Text="Up to date"
                                       Style="{StaticResource ValueStyle}"/>
                        </StackPanel>

                        <!-- Check for Updates Button -->
                        <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="2"
                                x:Name="CheckUpdateButton"
                                Content="Check for Updates"
                                Style="{StaticResource ActionButtonStyle}"
                                VerticalAlignment="Center"
                                Click="CheckUpdateButton_Click"/>

                        <!-- Update Progress (Hidden by default) -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                                    x:Name="UpdateProgressPanel"
                                    Margin="0,16,0,0"
                                    Visibility="Collapsed">
                            <TextBlock Text="Updating firmware..."
                                       Style="{StaticResource LabelStyle}"
                                       Margin="0,0,0,8"/>
                            <ProgressBar x:Name="UpdateProgressBar"
                                         Height="6"
                                         Background="{StaticResource CardBorder}"
                                         Foreground="{StaticResource AccentBrush}"
                                         Value="0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Power Management Card -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="Power Management" Style="{StaticResource SectionHeaderStyle}"/>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Sleep Mode -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Device Sleep Mode"
                                       Style="{StaticResource ValueStyle}"/>
                            <TextBlock Text="Automatically put device to sleep when inactive"
                                       Style="{StaticResource LabelStyle}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                        <ToggleButton Grid.Row="0" Grid.Column="1"
                                      x:Name="SleepModeToggle"
                                      Style="{StaticResource ToggleButtonStyle}"
                                      VerticalAlignment="Center"
                                      Checked="SleepModeToggle_Checked"
                                      Unchecked="SleepModeToggle_Unchecked"/>

                        <!-- Sleep Timer Settings (shown when sleep mode is enabled) -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                    x:Name="SleepTimerPanel"
                                    Margin="0,16,0,0"
                                    Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Sleep after:"
                                           Style="{StaticResource LabelStyle}"/>
                                <Slider Grid.Column="1"
                                        x:Name="SleepTimerSlider"
                                        Minimum="1" Maximum="60"
                                        Value="15"
                                        TickFrequency="5"
                                        IsSnapToTickEnabled="True"
                                        VerticalAlignment="Center"
                                        Margin="8,0"/>
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Value, ElementName=SleepTimerSlider, StringFormat={}{0:F0} min}"
                                           Style="{StaticResource ValueStyle}"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Connection Settings Card -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="Connection Settings" Style="{StaticResource SectionHeaderStyle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Offline Mode -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Offline Mode"
                                       Style="{StaticResource ValueStyle}"/>
                            <TextBlock Text="Disconnect device from network and run in standalone mode"
                                       Style="{StaticResource LabelStyle}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1"
                                      x:Name="OfflineModeToggle"
                                      Style="{StaticResource ToggleButtonStyle}"
                                      VerticalAlignment="Center"
                                      Checked="OfflineModeToggle_Checked"
                                      Unchecked="OfflineModeToggle_Unchecked"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Device Actions Card -->
            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="Device Actions" Style="{StaticResource SectionHeaderStyle}"/>

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="RestartDeviceButton"
                                Content="Restart Device"
                                Style="{StaticResource ActionButtonStyle}"
                                Background="{StaticResource WarningBrush}"
                                Click="RestartDeviceButton_Click"
                                Margin="0,0,12,0"/>
                        <Button x:Name="ResetSettingsButton"
                                Content="Reset to Factory Defaults"
                                Style="{StaticResource ActionButtonStyle}"
                                Background="#666666"
                                Click="ResetSettingsButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>